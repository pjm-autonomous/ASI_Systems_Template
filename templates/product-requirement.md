---
id: prodreq-
title:
parent-use-cases:
  -
priority:
requirement-type:
---

<!-- L0 artifact, owned by Compliance & Safety. Product performance AND safety
     requirements both use this type - the distinction is requirement-type, not
     a separate artifact type.

     Legal values for every enum field are in standard/artifact-schema.yaml.
     Do not restate them in prose; a prose copy drifts. -->

| Field | Value |
|---|---|
| ID | |
| Parent Use Cases | |
| Requirement Type | |
| Priority | |
| Verification Method | |

## Requirement Statement

<!-- EARS format:
       When <trigger>, the <system> shall <response>.
       While <state>, the <system> shall <behavior>.
       If <condition>, the <system> shall <action>.
       Where <feature is included>, the <system> shall <capability>. -->

## Rationale

<!-- Why this requirement exists, traced to the stakeholder need behind it. -->

## Acceptance Criteria

<!-- Observable, testable. If this requirement cites a bound (a timeout, a rate,
     a distance), that bound needs a param-*.md artifact with a real value - an
     unvalued bound makes the requirement unverifiable by construction. -->
