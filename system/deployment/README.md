# Deployment Architecture

Grouped by feature bucket, or `whole-system` for a project-wide topology doc: `system/deployment/<feature>/deploy-<description>.md`. Start from `templates/deployment-architecture.md`. See `example/system/deployment/` for a worked sample.
